LET the variable x BE the number 10;
LET the variable light BE the string "on";
PROCLAIM the value of x;
PROCLAIM the value of light;

IF the value of x IS greater than the number 5 THEN;
    PROCLAIM the value of light;
ELSE
    TERMINATE;
END IF;
